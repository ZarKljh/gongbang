'use client'

import React, { useEffect, useState } from 'react'

export default function StudioInfo(seller, studioList, activeTab, setActiveTab, selectedStudioId, setSelectedStudioId) {
    return <></>
}
